# mirrorneuron-prism

Bounded multi-call inference as a LiteLLM Proxy extension.

## Installation

```bash
pip install "mirrorneuron-prism[proxy]"
```

## Usage

See SPEC.md for full specification.

## Development

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e ".[proxy,dev]"
```
