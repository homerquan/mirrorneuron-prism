"""Output validation helpers (Step 01 baseline: STUB).

.. warning::
    :func:`validate_output` is a structural non-emptiness placeholder only.
    Non-empty output is NOT evidence of task correctness, and this stub
    performs no JSON-schema, tool-argument, or semantic verification. The
    real validation/verifier protocol (pass/fail/unknown + conclusive flag)
    lands in a later step.
"""


def validate_output(output: str) -> bool:
    return bool(output)
